export class AuthUser {
    constructor() {}
    
    public login = async ({userName, password}: {userName:string, password:string}) => {
      /*   const userExists = await this.verifyUser({userName})
        if (userExists) { */
    }
}